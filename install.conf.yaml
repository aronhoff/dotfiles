- clean: ['~']

- link:
    ~/.vim: vim/
    ~/.vimrc: vimrc
    ~/.zshrc: zshrc
    ~/.gitconfig: gitconfig
    ~/scripts: scripts
    ~/.gitignore: gitignore

- shell:
    - [git submodule update --init --recursive, Installing submodules]
